---
title: ldml
---

import { Callout } from 'nextra/components'

# `ldml`

The Unicode LDML (UTS #35) date-field grammar. Imported from `rosetta-date/dialects`:

```ts
import { ldml } from 'rosetta-date/dialects'
```

- **Spec:** [Unicode UTS #35 Date Field Symbol Table](https://www.unicode.org/reports/tr35/tr35-dates.html#Date_Field_Symbol_Table)
- **Family:** [`delimited`](/reference/dialects#syntax-families) — ASCII letters are token characters
- **Literals:** quotes `'...'`; a literal apostrophe is `''` — see [Literals & Adjacency](/guides/literals-and-adjacency)

## Tokens

Each token and the [canonical field](/reference/canonical-vocabulary) it maps to. For how these
compare across dialects, see [Token Mapping](/reference/token-mapping).

| Meaning | Token |
| ------- | ----- |
| Era, abbreviated | `GGG` |
| Era, wide | `GGGG` |
| Era, narrow | `GGGGG` |
| Calendar year | `yyyy` |
| Calendar year, 2-digit | `yy` |
| Local week-numbering year | `YYYY` |
| Local week-numbering year, 2-digit | `YY` |
| Quarter | `Q` |
| Quarter, 2-digit | `QQ` |
| Quarter, abbreviated | `QQQ` |
| Quarter, wide | `QQQQ` |
| Quarter, narrow | `QQQQQ` |
| Quarter, abbreviated, stand-alone | `qqq` |
| Quarter, wide, stand-alone | `qqqq` |
| Quarter, narrow, stand-alone | `qqqqq` |
| Month | `M` |
| Month, 2-digit | `MM` |
| Month, abbreviated | `MMM` |
| Month, wide | `MMMM` |
| Month, narrow | `MMMMM` |
| Month, abbreviated, stand-alone | `LLL` |
| Month, wide, stand-alone | `LLLL` |
| Month, narrow, stand-alone | `LLLLL` |
| Local week of year | `w` |
| Local week of year, 2-digit | `ww` |
| Day of month | `d` |
| Day of month, 2-digit | `dd` |
| Day of year | `D` |
| Day of year, 2-digit | `DD` |
| Day of year, 3-digit | `DDD` |
| Weekday, abbreviated | `EEE` |
| Weekday, wide | `EEEE` |
| Weekday, narrow | `EEEEE` |
| Weekday, short | `EEEEEE` |
| Weekday, abbreviated, stand-alone | `ccc` |
| Weekday, wide, stand-alone | `cccc` |
| Weekday, narrow, stand-alone | `ccccc` |
| Weekday, short, stand-alone | `cccccc` |
| Weekday, number (locale) | `e` |
| AM/PM | `a` |
| AM/PM, wide | `aaaa` |
| AM/PM, narrow | `aaaaa` |
| Hour 1–12 | `h` `hh` |
| Hour 0–23 | `H` `HH` |
| Hour 1–24 | `k` `kk` |
| Hour 0–11 | `K` `KK` |
| Minute | `m` `mm` |
| Second | `s` `ss` |
| Fractional second | `S` `SS` `SSS` |
| Time-zone name | `zzz` |
| Offset, `±hh:mm` | `xxx` |
| Offset, `±hhmm` | `xx` |

## Aliases

Extra spellings that are **parsed** but normalize to the primary token above when rendered:

- `y` / `yyy` → calendar year (like `yyyy`).
- `Y` / `YYY` → local week-numbering year (like `YYYY`).
- `G` / `GG` → abbreviated era (like `GGG`).
- `E` / `EE` → abbreviated weekday (like `EEE`).
- `eee` … `eeeeee` → formatting weekday name (like `EEE` … `EEEEEE`).
- `aa` / `aaa` → AM/PM (like `a`).
- `z` / `zz` → time-zone name (like `zzz`).

## Caveats

<Callout type="warning">
  - Case is significant: `y` (calendar year) and `Y` (local week-numbering year) are different
    fields, as are `d` (day of month) and `D` (day of year).
  - Ordinals (`Mo`, `Qo`, `wo`, `Do`…), ISO week fields, the Unix epoch, and localized presets are
    **not** part of the bare `ldml` dialect. A `dialect → dialect` conversion literalizes them; a
    [library](/reference/libraries) that extends `ldml` (e.g. date-fns) provides them.
</Callout>
