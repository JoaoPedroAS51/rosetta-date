---
title: Token Mapping
---

import { Callout } from 'nextra/components'

# Token Mapping

This is the **grammar layer** — how each dialect spells every [canonical
field](/reference/canonical-vocabulary). Each row is one canonical field; each column is a dialect.
This is the side-by-side comparison view; for one dialect's full grammar, literals, aliases, and
caveats, see its page under [Dialects](/reference/dialects).

<Callout type="info">
  A **—** means the dialect has no token for that field. Converting that field *into* the dialect
  has no clean target, so it is routed through [`onUnsupportedToken`](/guides/unsupported-tokens)
  (default: escaped as a literal). Adding a dialect adds a column — no row is pairwise.
</Callout>

Behaviour specific to a concrete tool (date-fns extensions, Day.js gaps, option-gated tokens) lives
in [Libraries](/reference/libraries); the tokens below are the dialect grammars themselves.

## Era & year

| Meaning | `moment` | `ldml` |
| ------- | -------- | ------ |
| Era, abbreviated | `N` | `GGG` |
| Era, wide | `NNNN` | `GGGG` |
| Era, narrow | `NNNNN` | `GGGGG` |
| Calendar year | `YYYY` | `yyyy` |
| Calendar year, 2-digit | `YY` | `yy` |
| Local week-numbering year | `gggg` | `YYYY` |
| Local week-numbering year, 2-digit | `gg` | `YY` |
| ISO week-numbering year | `GGGG` | — |
| ISO week-numbering year, 2-digit | `GG` | — |

## Month & quarter

| Meaning | `moment` | `ldml` |
| ------- | -------- | ------ |
| Quarter | `Q` | `Q` |
| Quarter, 2-digit | — | `QQ` |
| Quarter, ordinal | `Qo` | — |
| Quarter, abbreviated | — | `QQQ` |
| Quarter, wide | — | `QQQQ` |
| Quarter, narrow | — | `QQQQQ` |
| Quarter, abbreviated, stand-alone | — | `qqq` |
| Quarter, wide, stand-alone | — | `qqqq` |
| Quarter, narrow, stand-alone | — | `qqqqq` |
| Month | `M` | `M` |
| Month, 2-digit | `MM` | `MM` |
| Month, ordinal | `Mo` | — |
| Month, abbreviated | `MMM` | `MMM` |
| Month, wide | `MMMM` | `MMMM` |
| Month, narrow | — | `MMMMM` |
| Month, abbreviated, stand-alone | — | `LLL` |
| Month, wide, stand-alone | — | `LLLL` |
| Month, narrow, stand-alone | — | `LLLLL` |

## Week & day

| Meaning | `moment` | `ldml` |
| ------- | -------- | ------ |
| Local week of year | `w` | `w` |
| Local week of year, 2-digit | `ww` | `ww` |
| Local week of year, ordinal | `wo` | — |
| ISO week of year | `W` | — |
| ISO week of year, 2-digit | `WW` | — |
| ISO week of year, ordinal | `Wo` | — |
| Day of month | `D` | `d` |
| Day of month, 2-digit | `DD` | `dd` |
| Day of month, ordinal | `Do` | — |
| Day of year | `DDD` | `D` |
| Day of year, 2-digit | — | `DD` |
| Day of year, 3-digit | `DDDD` | `DDD` |
| Day of year, ordinal | `DDDo` | — |

## Weekday

| Meaning | `moment` | `ldml` |
| ------- | -------- | ------ |
| Weekday, abbreviated | `ddd` | `EEE` |
| Weekday, wide | `dddd` | `EEEE` |
| Weekday, narrow | — | `EEEEE` |
| Weekday, short | `dd` | `EEEEEE` |
| Weekday, abbreviated, stand-alone | — | `ccc` |
| Weekday, wide, stand-alone | — | `cccc` |
| Weekday, narrow, stand-alone | — | `ccccc` |
| Weekday, short, stand-alone | — | `cccccc` |
| Weekday, number (locale) | `d` | `e` |
| Weekday, number (ISO) | `E` | — |

## Day period & time

| Meaning | `moment` | `ldml` |
| ------- | -------- | ------ |
| AM/PM | `A` | `a` |
| AM/PM, wide | — | `aaaa` |
| AM/PM, narrow | — | `aaaaa` |
| Hour 1–12 | `h` | `h` |
| Hour 1–12, 2-digit | `hh` | `hh` |
| Hour 0–23 | `H` | `H` |
| Hour 0–23, 2-digit | `HH` | `HH` |
| Hour 1–24 | `k` | `k` |
| Hour 1–24, 2-digit | `kk` | `kk` |
| Hour 0–11 | — | `K` |
| Hour 0–11, 2-digit | — | `KK` |
| Minute | `m` | `m` |
| Minute, 2-digit | `mm` | `mm` |
| Second | `s` | `s` |
| Second, 2-digit | `ss` | `ss` |
| Fractional second (1–3 digits) | `S` `SS` `SSS` | `S` `SS` `SSS` |

## Time zone & epoch

| Meaning | `moment` | `ldml` |
| ------- | -------- | ------ |
| Time-zone name | `z` | `zzz` |
| Offset, `±hh:mm` | `Z` | `xxx` |
| Offset, `±hhmm` | `ZZ` | `xx` |
| Unix timestamp, seconds | `X` | — |
| Unix timestamp, milliseconds | `x` | — |

<Callout type="info">
  The `ldml` dialect itself defines no Unix-epoch tokens — those are date-fns extensions
  (`t` / `T`), so they convert only through the `dateFns` library. The same applies to ISO week
  fields and ordinals, which are not part of the bare dialect. See
  [dateFns](/reference/libraries/datefns).
</Callout>

## Localized presets

These render per the library's loaded locale and map **preset → preset**. Not every dialect defines
them: where a column shows `—` here, the presets are supplied by a *library* layered on that dialect
(e.g. `dateFns` over `ldml`), not by the bare dialect itself. See
[Libraries → Localized presets](/reference/libraries#localized-presets).

| Meaning | `moment` | `ldml` |
| ------- | -------- | ------ |
| Date, short | `L` | — |
| Date, medium | `ll` | — |
| Date, long | `LL` | — |
| Time, short | `LT` | — |
| Time, with seconds | `LTS` | — |
| Date + time, medium | `lll` | — |
| Date + time, long | `LLL` | — |
| Date + time, full | `LLLL` | — |

## Aliases & caveats

Extra spellings that parse but normalize to a primary token, plus dialect-specific quirks (numbering,
casing), are documented per dialect — they are properties of one grammar, not of a pair:

- [`moment`](/reference/dialects/moment)
- [`ldml`](/reference/dialects/ldml)
